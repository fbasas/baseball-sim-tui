---
phase: 05-narrative-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tui/styles/game.tcss
  - src/tui/widgets/situation.py
  - src/tui/app.py
autonomous: true
requirements: [TUI-06]

must_haves:
  truths:
    - "Dashboard has dark green background with cream panels and brown borders"
    - "Situation panel shows visual base diamond with occupied/empty indicators"
    - "Footer bar displays key binding reminders (Space, S, F, Q)"
    - "Different panel types have distinct border styles (heavy, double, tall, rounded)"
  artifacts:
    - path: "src/tui/styles/game.tcss"
      provides: "Classic baseball color theme with distinct border styles"
      contains: "#1a3a1a"
    - path: "src/tui/widgets/situation.py"
      provides: "Visual base diagram in situation panel"
      contains: "base_diagram"
  key_links:
    - from: "src/tui/styles/game.tcss"
      to: "src/tui/screens/game_screen.py"
      via: "CSS_PATH loading"
      pattern: "game.tcss"
    - from: "src/tui/widgets/situation.py"
      to: "src/game/state.py"
      via: "update_from_state() with BaseState"
      pattern: "update_from_state"
---

<objective>
Apply the classic baseball TCSS color theme (dark green field, cream panels, brown borders, gold accents) and upgrade the situation panel with a visual base diamond. Add footer bar with key binding reminders.

Purpose: Transform the raw Textual default appearance into a polished baseball aesthetic that evokes the ballpark.
Output: Overhauled game.tcss, upgraded situation widget with base diamond, footer key bindings.
</objective>

<execution_context>
@/home/fbasas/.claude/get-shit-done/workflows/execute-plan.md
@/home/fbasas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-narrative-polish/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/tui/styles/game.tcss (current):
```css
/* Current: uniform solid $secondary borders, default Textual colors */
/* Will be completely overhauled */
```

From src/tui/widgets/situation.py:
```python
class SituationWidget(Static):
    def update_from_state(self, inning: int, half: InningHalf, outs: int, bases: BaseState) -> None: ...
```

From src/game/state.py:
```python
@dataclass(frozen=True)
class BaseState:
    first: Optional[str] = None   # player_id or None
    second: Optional[str] = None
    third: Optional[str] = None
```

From src/tui/app.py:
```python
class BaseballSimApp(App):
    CSS_PATH = "styles/game.tcss"
    BINDINGS = [
        ("space", "advance", "Next Play"),
        ("enter", "advance", "Next Play"),
        ("f", "fast_forward", "Fast Forward"),
        ("s", "substitution", "Substitutions"),
        ("q", "quit", "Quit"),
    ]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TCSS baseball color theme and border styles</name>
  <files>src/tui/styles/game.tcss</files>
  <action>
Overhaul `src/tui/styles/game.tcss` with the classic baseball color palette. Read the existing file first, then rewrite while preserving the grid layout structure.

Color palette per user decisions:
- Screen background: #1a3a1a (dark green field)
- Boxscore panel: background #2d5a1b (medium green), text #fffdd0 (cream), border heavy #8b4513 (brown)
- Lineup cards (#away-lineup, #home-lineup): background #f5f0dc (cream/parchment), text #2c1810 (dark brown), border tall #8b4513 (brown)
- Center panel: background #1a3a1a (dark green)
- Play log: background #0d1f0d (very dark green), border round #4a7c3f (mid green)
- Situation panel: background #2d5a1b, border double #ffd700 (gold accent)
- Score flash (#boxscore.score-changed): background #ffd700 (yellow), text #1a3a1a

Border style variety per user decision:
- #boxscore: heavy (thick single line)
- #situation: double (double line, gold)
- #away-lineup, #home-lineup: tall (extended height caps)
- #play-log: round (rounded corners)

Additional polish:
- Panel padding: 1 unit internal padding for readability
- Lineup card formatting: monospace-friendly alignment
- Header text in boxscore: bold styling

Preserve the existing 3-column grid layout (left lineup, center game area, right lineup) and docked boxscore structure. Only change colors, borders, and padding.
  </action>
  <verify>
    <automated>python -c "from src.tui.app import BaseballSimApp; app = BaseballSimApp(); print('CSS loads OK')"</automated>
  </verify>
  <done>game.tcss has baseball color theme with dark green background, cream panels, brown borders, gold situation border, and distinct border styles per panel type.</done>
</task>

<task type="auto">
  <name>Task 2: Visual base diamond and footer key bindings</name>
  <files>src/tui/widgets/situation.py, src/tui/app.py</files>
  <action>
1. **Base diamond in SituationWidget** (`src/tui/widgets/situation.py`):
   - Modify the `update_from_state()` method (or the render/update method) to include an ASCII base diamond
   - Diamond layout using Rich markup in a Static widget:
     ```
         [2B]
        /    \
     [3B]    [1B]
        \    /
         [H]
     ```
   - Use filled indicator for occupied base (e.g., Rich markup `[bold yellow]` or `[reverse]` for occupied, `[dim]` for empty)
   - Example: if runner on 1st and 3rd: 1B and 3B are highlighted, 2B is dim
   - Keep the existing inning number, half indicator (Top/Bot), and outs display
   - Add the diamond below or alongside the existing text
   - Approach per Claude's discretion but must be visible and clear

2. **Footer key bindings** (`src/tui/app.py`):
   - The existing BINDINGS list already populates Textual's footer automatically
   - Ensure each binding has a clear description label:
     - ("space", "advance", "Next Play")
     - ("f", "fast_forward", "Fast Forward")
     - ("s", "substitution", "Substitutions")
     - ("q", "quit", "Quit")
   - If the footer isn't showing, add `from textual.widgets import Footer` and include `Footer()` in the app's compose method or the GameScreen compose
   - Style the footer in game.tcss to match the baseball theme (dark background, cream text)

3. **Boxscore header polish:** In game.tcss, ensure the boxscore header area has appropriate styling (bold team names, clear column alignment).
  </action>
  <verify>
    <automated>python -c "from src.tui.widgets.situation import SituationWidget; w = SituationWidget(); print('Situation widget loads OK')" && python -c "from src.tui.app import BaseballSimApp; print('App with footer loads OK')"</automated>
  </verify>
  <done>Situation panel shows visual base diamond with occupied/empty indicators. Footer bar displays key binding reminders. Boxscore header is polished.</done>
</task>

</tasks>

<verification>
- `python -c "from src.tui.app import BaseballSimApp; print('OK')"` -- app loads without CSS errors
- `python -m pytest tests/ -x -q` -- no regression in existing tests
- Visual verification needed at checkpoint in Plan 04
</verification>

<success_criteria>
- game.tcss has complete baseball color theme (dark green, cream, brown, gold)
- Each panel type has a distinct border style (heavy, double, tall, round)
- Situation widget renders ASCII base diamond showing occupied/empty bases
- Footer bar visible with Space/S/F/Q key bindings
- No regression in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/05-narrative-polish/05-02-SUMMARY.md`
</output>
