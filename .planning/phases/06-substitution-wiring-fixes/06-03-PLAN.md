---
phase: 06-substitution-wiring-fixes
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/tui/screens/substitution_menu.py
  - src/tui/screens/game_screen.py
autonomous: false
requirements:
  - SUBS-02
  - SUBS-03
tags: [tui, substitution-menu, pinch-hitter, validation, replay, wiring]

must_haves:
  truths:
    - "User can select a pinch hitter from the substitution menu — bench player rows appear and are clickable"
    - "Confirming a pinch hitter selection dismisses the menu with a ('pinch_hitter', current_batter_id, selected_batter_id) tuple"
    - "All substitutions (pitching and pinch hit) go through GameEngine.make_substitution; invalid subs (e.g. already-removed player) raise ValueError surfaced to the play log"
    - "After Replay/New Game from the end-game menu, sub_manager is a fresh SubstitutionManager — previously-removed players are again available"
    - "After a pinch hit, the lineup card refreshes to show the new batter"
  artifacts:
    - path: "src/tui/screens/substitution_menu.py"
      provides: "Two scrollable PlayerListItem lists — Pitchers and Batters — with a sub-type toggle/selection that returns 'pitching_change' OR 'pinch_hitter' on confirm"
      contains: "batter-list"
    - path: "src/tui/screens/game_screen.py"
      provides: "_handle_substitution() routes through engine.make_substitution; _reset_game() reinitializes sub_manager"
      contains: "self.engine.make_substitution"
  key_links:
    - from: "src/tui/screens/game_screen.py:_handle_substitution"
      to: "src/game/engine.py:GameEngine.make_substitution"
      via: "validated substitution path"
      pattern: "self\\.engine\\.make_substitution\\("
    - from: "src/tui/screens/game_screen.py:_reset_game"
      to: "SubstitutionManager()"
      via: "fresh manager on replay"
      pattern: "self\\.sub_manager = SubstitutionManager\\("
    - from: "src/tui/screens/substitution_menu.py:compose"
      to: "batter PlayerListItem widgets"
      via: "second VerticalScroll list"
      pattern: "b-\\{"
---

<objective>
Close the three TUI-side audit gaps (3, 4, 5):
- Gap 3 (SUBS-02): SubstitutionMenu.compose() currently only yields pitcher widgets. The batters list is constructed by GameScreen.show_substitution_menu but never rendered. Render it.
- Gap 5 (SUBS-03): GameScreen._handle_substitution calls sub_manager.record_substitution() directly, bypassing GameEngine.make_substitution()'s validation (no-re-entry, DH forfeiture from Plan 02). Route through the engine.
- Gap 4 (SUBS-03): GameScreen._reset_game() leaves the old SubstitutionManager attached, so removed players carry over into a replayed game. Replace it.

Depends on 06-01 (engine simulation correctness so a pinch-hit batter's stats actually drive subsequent at-bats) and 06-02 (make_substitution now sets dh_forfeited correctly, so routing through it gives the right rule behavior).

Purpose: After this plan, SUBS-01/02/03 are end-to-end functional through the TUI.
Output: Substitution modal renders both lists; one validated code path executes subs; replay resets state. Includes one human verification checkpoint because this is the end-user-facing change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/04-substitutions-advanced-mechanics/04-04-SUMMARY.md
@.planning/phases/04-substitutions-advanced-mechanics/04-05-SUMMARY.md
@src/tui/screens/substitution_menu.py
@src/tui/screens/game_screen.py
@src/game/engine.py
@src/game/substitutions.py
@src/game/state.py

<interfaces>
<!-- Existing contracts the executor needs -->

From src/tui/screens/substitution_menu.py:
  - SubstitutionMenu(ModalScreen[Optional[Tuple[str, str, str]]]).__init__ already accepts both `pitchers` and `batters` lists; only `pitchers` are rendered in compose() (lines 166-176).
  - PlayerListItem widget posts PlayerSelected message with widget_id prefix "p-" or "b-"; on_player_selected at lines 179-189 already discriminates pitchers vs batters via prefix.
  - Return tuple shape: (sub_type, player_out_id, player_in_id) where sub_type in {"pitching_change", "pinch_hitter"}.
  - CSS already defines #pitcher-list; add a parallel #batter-list with similar height/border.

From src/tui/screens/game_screen.py:
  - show_substitution_menu (lines 692-763) already builds both pitchers and batters lists and passes them to SubstitutionMenu.
  - _handle_substitution (lines 765-899) — the broken path. Currently mutates state and lineup directly and calls sub_manager.record_substitution(). Must route through self.engine.make_substitution.
  - _reset_game (lines 621-649) — clears most state but does NOT reset self.sub_manager.
  - self.engine is the GameEngine instance constructed in _finalize_game_setup (line 179); currently constructed WITHOUT a SubstitutionManager. Must be updated so GameEngine has self.sub_manager so its validation runs.

From src/game/engine.py:GameEngine.make_substitution (after Plan 02):
  - Signature: (state, team, is_away_team, player_out_id, player_in_id, new_position=None, is_pitching_change=False) -> (GameState, Team)
  - Raises ValueError on invalid substitution (re-entry, etc.)
  - For pitching changes: returns new_state with updated pitcher_id and fresh FatigueState
  - For position players: mutates team.lineup via team.update_lineup_slot and returns (state, team) unchanged otherwise

From src/game/substitutions.py:
  - SubstitutionManager() default constructor — call with no args to get a fresh one with both DH flags True.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render batter list in SubstitutionMenu and toggle sub-type on selection</name>
  <files>src/tui/screens/substitution_menu.py</files>
  <read_first>
    - src/tui/screens/substitution_menu.py (compose at lines 166-177; on_player_selected at lines 179-189; on_button_pressed at lines 191-206; action_confirm at lines 211-222; CSS at lines 83-135)
    - src/tui/screens/game_screen.py:show_substitution_menu (lines 692-763) to confirm the `batters` arg shape: List[Tuple[str, str, str, bool]] of (id, name, slash_line, is_available)
  </read_first>
  <action>
    Modify compose() to yield TWO labeled VerticalScroll lists: one for relievers (existing, id="pitcher-list") and one for bench batters (new, id="batter-list"). Each batter PlayerListItem must use id=f"b-{pid}" so the existing on_player_selected discrimination still works. Add CSS rules for #batter-list mirroring #pitcher-list (height: 8 or split the 10-row pitcher region into two 5-row regions; allow the modal container height to grow to 28 to fit both lists plus title plus button row). Keep the title and button row as-is. In on_button_pressed and action_confirm, replace the pitcher-only auto-confirm logic so that:
      - If self._selected_pitcher is set AND self._selected_batter is NOT, dismiss with ("pitching_change", self._current_pitcher, self._selected_pitcher)
      - Else if self._selected_batter is set AND self._selected_pitcher is NOT, dismiss with ("pinch_hitter", self._current_batter, self._selected_batter)
      - Else if both are set, prefer the most recently selected — track via a `self._last_selection: Optional[str]` attribute set in on_player_selected to "pitcher" or "batter".
      - Else (neither selected) dismiss with None.
    Remove the auto-pick-first-pitcher fallback that currently runs when nothing is selected — that fallback masks UX bugs and produces unintended substitutions. Update the title label to "═══ SUBSTITUTIONS ═══" (already correct) and add two section labels: "[bold]Available Relievers:[/bold]" (already present) and "[bold]Available Pinch Hitters:[/bold]" (new).
  </action>
  <verify>
    <automated>grep -n "batter-list" src/tui/screens/substitution_menu.py</automated>
    <automated>grep -n 'id=f"b-{' src/tui/screens/substitution_menu.py</automated>
    <automated>grep -n "pinch_hitter" src/tui/screens/substitution_menu.py</automated>
    <automated>python -c "from src.tui.screens.substitution_menu import SubstitutionMenu; m = SubstitutionMenu(pitchers=[('p1','Reliever A',3.50,True)], batters=[('b1','Bench Bat',0.300,True)], current_pitcher_id='p0', current_batter_id='b0'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/tui/screens/substitution_menu.py:compose() yields a VerticalScroll with id="batter-list" containing PlayerListItem widgets with ids prefixed "b-"
    - on_button_pressed and action_confirm dispatch to either "pitching_change" OR "pinch_hitter" based on which list the user selected from (no auto-pick-first fallback)
    - python -c instantiation smoke test exits 0 (no syntax/import errors)
    - All four grep commands above return non-empty output
  </acceptance_criteria>
  <done>
    Both pitcher and batter lists render in the modal. Selecting from each list produces the correct sub_type tuple on confirm. No auto-fallback masking selection failures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Route _handle_substitution through GameEngine.make_substitution and reset sub_manager on replay</name>
  <files>src/tui/screens/game_screen.py</files>
  <read_first>
    - src/tui/screens/game_screen.py (full file — focus on _finalize_game_setup line ~179, _handle_substitution lines 765-899, _reset_game lines 621-649, __init__ line 71 where self.sub_manager is created)
    - src/game/engine.py:GameEngine.__init__ (signature accepts substitution_manager parameter) and GameEngine.make_substitution (signature, return type, raises ValueError)
    - src/game/positions.py (Position enum, DesignatedHitter sentinel)
    - src/game/substitutions.py (SubstitutionManager default constructor)
  </read_first>
  <action>
    Step 1 — Wire GameEngine to share self.sub_manager. In _finalize_game_setup, change `self.engine = GameEngine()` to `self.engine = GameEngine(substitution_manager=self.sub_manager)`. This makes the engine's validate_pitching_change/validate_pinch_hitter actually fire against the screen's manager.

    Step 2 — Replace the body of _handle_substitution. After unpacking sub_type, player_out_id, player_in_id and determining batting_team/fielding_team from state.half (existing logic at lines 778-786 is correct, keep it), call:
      try:
        new_state, modified_team = self.engine.make_substitution(
          state=state,
          team=<fielding_team if sub_type == 'pitching_change' else batting_team>,
          is_away_team=<True if (sub_type=='pitching_change' and state.half==InningHalf.BOTTOM) or (sub_type=='pinch_hitter' and state.half==InningHalf.TOP) else False>,
          player_out_id=player_out_id,
          player_in_id=player_in_id,
          new_position=None if sub_type == 'pinch_hitter' else Position.PITCHER,
          is_pitching_change=(sub_type == 'pitching_change'),
        )
      except ValueError as e:
        log.add_play(f"[bold red]Invalid substitution: {e}[/bold red]")
        return
      self.game_state = new_state
    The fielding-vs-batting team selection and is_away_team derivation must match: pitching change targets the fielding team (home team during TOP, away during BOTTOM — is_away_team=False during TOP, True during BOTTOM). Pinch hitter targets the batting team (away during TOP, home during BOTTOM — is_away_team=True during TOP, False during BOTTOM).

    Step 3 — Drop the duplicate logic. Delete the manual dataclass_replace pitcher swaps (lines 797-810), the manual SubstitutionRecord construction and sub_manager.record_substitution call (lines 813-823), the manual lineup-slot mutation block (lines 847-872), and the manual record_substitution for pinch hitters (lines 875-885). These are all now done by GameEngine.make_substitution. Keep the narrative logging (generate_substitution_text, generate_pinch_hitter_text) and the _pitcher_consecutive_retired reset and the lineup card refresh at line 899 — those are presentation concerns, not state concerns.

    Step 4 — Reset sub_manager on replay. In _reset_game (after the existing field resets), add: `self.sub_manager = SubstitutionManager()`. Then re-wire the engine to use the new manager: `if self.engine is not None: self.engine.sub_manager = self.sub_manager`. This ensures a Replay produces a clean substitution state and the engine validates against the fresh manager. Do NOT reload the teams or rebuild lineups — current _reset_game intentionally keeps the team rosters; only fresh sub_manager + cleared game state is needed.
  </action>
  <verify>
    <automated>grep -n "self.engine.make_substitution" src/tui/screens/game_screen.py</automated>
    <automated>grep -n "self.sub_manager = SubstitutionManager" src/tui/screens/game_screen.py</automated>
    <automated>grep -nE "sub_manager\.record_substitution" src/tui/screens/game_screen.py</automated>
    <automated>grep -n "GameEngine(substitution_manager=self.sub_manager)" src/tui/screens/game_screen.py</automated>
    <automated>pytest tests/ -x</automated>
    <automated>python -c "from src.tui.screens.game_screen import GameScreen; print('import-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - grep "self.engine.make_substitution" returns at least one match inside _handle_substitution
    - grep "self.sub_manager = SubstitutionManager" returns at least two matches (one in __init__, one in _reset_game)
    - grep "sub_manager.record_substitution" returns ZERO matches in src/tui/screens/game_screen.py (engine now owns recording)
    - grep "GameEngine(substitution_manager=self.sub_manager)" returns one match in _finalize_game_setup
    - GameScreen still imports without errors (python -c smoke test exits 0)
    - Full pytest tests/ suite still passes — no test file depends on the removed duplicate logic
  </acceptance_criteria>
  <done>
    All TUI substitutions flow through engine.make_substitution. _reset_game produces a fresh sub_manager and the engine references it. No direct sub_manager.record_substitution call remains in the TUI layer.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end verification — pinch hitter, pitching change, replay reset</name>
  <files>src/tui/screens/substitution_menu.py, src/tui/screens/game_screen.py</files>
  <what-built>
    Plans 06-01 and 06-02 fixed the simulation side; this plan wired the TUI. End-to-end:
    - Pinch hitter list is visible in the substitution modal and selecting confirms with sub_type='pinch_hitter'
    - All substitutions route through GameEngine.make_substitution (no-re-entry enforced; DH forfeiture detected)
    - Replay produces a fresh substitution state
    - Pitching changes affect subsequent at-bats (new pitcher's stats used)
  </what-built>
  <action>
    Human-only verification step. The engineer runs the TUI and walks through the five scenarios in how-to-verify. No code changes occur in this task.
  </action>
  <how-to-verify>
    Run `python -m src.tui.app` and complete the pitcher selection flow for 1927 NYA vs CHN. Then:

    1. PINCH HITTER VISIBILITY (Gap 3 / SUBS-02):
       - Press S to open the substitution menu
       - Verify BOTH "Available Relievers" and "Available Pinch Hitters" sections appear with player rows
       - Click a bench batter row — verify the row is the one selected (background should change)
       - Press C to confirm
       - Expected: play log shows a pinch hitter narrative line; lineup card for the batting team updates to show the new player at the current batting slot

    2. PITCHING CHANGE EFFECTS SIM (SUBS-01, depends on Plan 01):
       - Press S, select a reliever from "Available Relievers", press C
       - Verify play log shows "Pitching change: ..." narrative
       - Verify the fatigue widget resets to 0%
       - Verify the displayed pitcher name in the fatigue widget changes to the new pitcher
       - Press Space several times to advance at-bats; verify play log continues with the new pitcher's name

    3. NO-REENTRY VALIDATION (Gap 5 / SUBS-03):
       - After a substitution, press S again
       - Verify the just-removed player appears grayed out with "(Used)" suffix in their list
       - Confirm rule enforcement: the engine will raise ValueError if you somehow try to re-insert; expected behavior is that the UI prevents this via the (Used) gray-out

    4. REPLAY RESETS SUBSTITUTIONS (Gap 4 / SUBS-03):
       - Press F to fast-forward to game end
       - From the box score screen, select Replay
       - After replay starts, press S
       - Verify the previously-removed players are NOT grayed (the sub_manager was reset)

    5. NO REGRESSIONS:
       - The boxscore, situation widget, play log, and lineup cards still render correctly
       - Fast-forward still works
       - End-of-game box score still appears
  </how-to-verify>
  <verify>
    <human-check>All five scenarios above pass when the engineer runs python -m src.tui.app and exercises the substitution flow.</human-check>
  </verify>
  <done>
    Engineer types "approved" after confirming all five scenarios. Any failure is described and routed back to Task 1 or Task 2 for repair before resuming.
  </done>
  <resume-signal>Type "approved" if all five scenarios pass, or describe failures so the engineer can fix them.</resume-signal>
</task>

</tasks>

<threat_model>
Local single-player TUI game with no network surface, no auth, no user-supplied data ingestion at runtime. Threats N/A for local TUI logic. No new packages.
</threat_model>

<verification>
1. `pytest tests/ -x` — full suite passes (no Textual tests broken)
2. `grep -n "self.engine.make_substitution\|self.sub_manager = SubstitutionManager" src/tui/screens/game_screen.py` — both engine routing and reset visible
3. `grep -c "sub_manager.record_substitution" src/tui/screens/game_screen.py | grep -v "^#" | head -1` — must be 0 (no direct record_substitution calls in the TUI layer)
4. Human verification checkpoint (Task 3) confirms all five end-user scenarios
</verification>

<success_criteria>
- Substitution menu renders both relievers and pinch hitters
- All substitutions route through GameEngine.make_substitution (validation enforced, DH forfeiture detected)
- _reset_game produces a fresh SubstitutionManager
- Pitching changes affect subsequent at-bat simulation (Plan 01 wiring + Plan 03 routing)
- Existing pytest suite passes
- Human verifies the five end-to-end scenarios
</success_criteria>

<output>
Create `.planning/phases/06-substitution-wiring-fixes/06-03-SUMMARY.md` when done — list the three TUI fixes, the human verification outcome, and call out any deferred items (e.g. pinch runner UI is still out of scope).
</output>
