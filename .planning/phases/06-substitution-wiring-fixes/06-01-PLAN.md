---
phase: 06-substitution-wiring-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/engine.py
  - src/tui/screens/game_screen.py
  - tests/test_game_engine.py
autonomous: true
requirements:
  - SUBS-01
tags: [engine, fatigue, pitching-change, simulation]

must_haves:
  truths:
    - "After a pitching change, the next at-bat simulates using the new pitcher's stats (not the starter's)"
    - "When a pitcher's fatigue is non-zero, simulate_at_bat() receives fatigue-modified PitchingStats (more hits, walks, HRs allowed)"
    - "A high-fatigue pitcher (close to 1.0) produces measurably more hits over 1000 at-bats than the same pitcher at 0.0 fatigue"
  artifacts:
    - path: "src/game/engine.py"
      provides: "simulate_half_inning() resolves pitching_stats from state.current_pitcher_id and applies fatigue modifier"
      contains: "apply_fatigue_modifier"
    - path: "src/tui/screens/game_screen.py"
      provides: "advance_game() reads pitcher from state.current_pitcher_id, not lineup.starting_pitcher_id"
      contains: "current_pitcher_id"
    - path: "tests/test_game_engine.py"
      provides: "Regression tests proving pitcher-change-affects-sim and fatigue-affects-sim"
      contains: "TestPitcherChangeEffectsSim"
  key_links:
    - from: "src/tui/screens/game_screen.py:advance_game"
      to: "state.current_pitcher_id"
      via: "stats lookup after pitching change"
      pattern: "state\\.(home|away)_pitcher_id"
    - from: "src/game/engine.py:simulate_half_inning"
      to: "apply_fatigue_modifier"
      via: "pre-simulation stats adjustment"
      pattern: "apply_fatigue_modifier\\("
---

<objective>
Fix the two simulation-side wiring bugs that make pitching changes and fatigue cosmetic. After this plan, the at-bat simulation actually uses (a) the current pitcher in GameState (not the lineup's frozen starting_pitcher_id) and (b) fatigue-modified pitching stats — so substitutions and pitcher tiredness produce measurable changes in outcomes.

Purpose: Close audit gaps 1 and 2 (SUBS-01). Without this, the rest of Phase 6 (UI wiring, DH rules) operates on a sim that ignores its inputs.
Output: GameEngine.simulate_half_inning applies fatigue modifier; GameScreen.advance_game resolves pitcher from state; two regression tests prove both behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@src/game/engine.py
@src/game/state.py
@src/game/fatigue.py
@src/tui/screens/game_screen.py
@tests/test_game_engine.py

<interfaces>
<!-- Key contracts the executor needs. Already defined in the codebase. -->

From src/game/state.py — GameState properties relevant to pitcher lookup:
  - state.current_pitcher_id -> Optional[str]   (resolves to home_pitcher_id during TOP, away_pitcher_id during BOTTOM)
  - state.current_pitcher_fatigue -> FatigueState
  - state.home_pitcher_id / state.away_pitcher_id (raw fields)
  - state.with_pitcher(pitcher_id, fatigue) returns new GameState

From src/game/engine.py — already defined, currently uncalled in the hot path:
  - apply_fatigue_modifier(pitching_stats: PitchingStats, fatigue: float) -> PitchingStats
    (multipliers per session log: hits *= 1 + fatigue*0.5, walks *= 1 + fatigue*0.3, HRs *= 1 + fatigue*0.4)
  - GameEngine.simulate_half_inning(state, batting_lineup, pitching_stats, park_factor=100) — currently accepts pitching_stats from caller and never applies fatigue
  - GameEngine._apply_result(state, result) — already updates fatigue AFTER the at-bat

From src/game/fatigue.py:
  - FatigueState dataclass: batters_faced, times_through_order, stress_events, current_fatigue (0.0-1.0)
  - calculate_fatigue(state, config=None) -> float (recomputes from state)

From src/tui/screens/game_screen.py:advance_game (lines ~388-398) — the broken section:
  - Currently does: pitcher_id = pitching_team.lineup.starting_pitcher_id
  - Must change to: pitcher_id = state.current_pitcher_id (or state.home_pitcher_id / state.away_pitcher_id mirroring the InningHalf branch already above)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply fatigue modifier inside simulate_half_inning</name>
  <files>src/game/engine.py, tests/test_game_engine.py</files>
  <read_first>
    - src/game/engine.py (current simulate_half_inning at lines 351-390; apply_fatigue_modifier at lines 32-57; _apply_result at lines 272-349 which already updates fatigue post-AB)
    - src/game/fatigue.py (FatigueState shape, calculate_fatigue signature)
    - src/game/state.py (current_pitcher_fatigue property)
    - tests/test_game_engine.py (existing TestGameEngine / TestSimulateHalfInning patterns, make_pitching_stats fixture)
    - tests/test_fatigue.py (test patterns for fatigue calculations)
  </read_first>
  <behavior>
    - Test 1 (TestFatigueEffectsSim::test_fatigue_modifier_called_before_at_bat): With a state whose current_pitcher_fatigue.current_fatigue == 0.8, simulate_half_inning must internally use a PitchingStats whose hits_allowed equals int(base_hits * 1.4) (1 + 0.8*0.5). Verify by monkey-patching SimulationEngine.simulate_at_bat to capture the pitching_stats argument and asserting hits_allowed == int(base * 1.4).
    - Test 2 (TestFatigueEffectsSim::test_zero_fatigue_passes_stats_unchanged): With current_pitcher_fatigue.current_fatigue == 0.0, captured pitching_stats.hits_allowed must equal the base value (multiplier 1.0, no change).
    - Test 3 (TestFatigueEffectsSim::test_high_fatigue_increases_hits_over_many_ats): Run 1000 at-bats with state.current_pitcher_fatigue=FatigueState(current_fatigue=0.9) vs FatigueState(current_fatigue=0.0). Assert hit count at 0.9 > hit count at 0.0 (with a seeded RNG). Skip-marker acceptable if SimulationEngine is non-deterministic for this composition; minimum required: Tests 1 and 2.
  </behavior>
  <action>
    Modify GameEngine.simulate_half_inning so that immediately before each call to self.sim.simulate_at_bat, it wraps the incoming pitching_stats with apply_fatigue_modifier using current_state.current_pitcher_fatigue.current_fatigue. The fatigue value used must come from the CURRENT loop iteration's state (not the entry-time state) because _apply_result updates fatigue after each AB and the loop reuses current_state. Pass the modified stats — not the original — into self.sim.simulate_at_bat. Do not change the function signature; callers continue passing the unmodified base pitching_stats and the engine handles modification internally. In tests/test_game_engine.py, add class TestFatigueEffectsSim with the three behaviors above, reusing make_pitching_stats and make_lineup fixtures. Use unittest.mock.patch or direct attribute assignment to intercept self.sim.simulate_at_bat and capture the pitching_stats arg.
  </action>
  <verify>
    <automated>pytest tests/test_game_engine.py::TestFatigueEffectsSim -x -v</automated>
    <automated>pytest tests/test_game_engine.py -x</automated>
    <automated>grep -n "apply_fatigue_modifier" src/game/engine.py | grep -v "def apply_fatigue_modifier"</automated>
  </verify>
  <acceptance_criteria>
    - src/game/engine.py contains a call to apply_fatigue_modifier inside the simulate_half_inning while-loop (grep shows at least one call site beyond the definition)
    - pytest tests/test_game_engine.py::TestFatigueEffectsSim::test_fatigue_modifier_called_before_at_bat exits 0
    - pytest tests/test_game_engine.py::TestFatigueEffectsSim::test_zero_fatigue_passes_stats_unchanged exits 0
    - All previously passing tests in tests/test_game_engine.py still pass (no regressions in the 19 existing tests)
    - simulate_half_inning's public signature unchanged (still accepts state, batting_lineup, pitching_stats, park_factor)
  </acceptance_criteria>
  <done>
    Fatigue is applied inside the simulation loop. Pitching stats handed to simulate_at_bat reflect current fatigue. Two new tests pass. No existing tests broken.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GameScreen.advance_game looks up pitcher from GameState</name>
  <files>src/tui/screens/game_screen.py, tests/test_game_engine.py</files>
  <read_first>
    - src/tui/screens/game_screen.py (advance_game at lines 348-432, specifically the pitcher_id assignment at line 397)
    - src/game/state.py (current_pitcher_id property; home_pitcher_id / away_pitcher_id fields)
    - src/game/team.py:Team (pitching_stats dict keyed by player_id)
    - tests/test_game_engine.py (TestGameEngine class patterns)
  </read_first>
  <behavior>
    - Test 1 (TestPitcherChangeEffectsSim::test_advance_game_uses_state_pitcher_after_substitution): Construct two Team objects with known pitching_stats dicts containing distinct starter and reliever entries. Build a GameState whose home_pitcher_id points to the RELIEVER (not the lineup.starting_pitcher_id). Call the lookup logic that mirrors advance_game (extract to a small helper if convenient OR test the inline branch directly). Assert that the resolved pitching_stats object IS pitching_team.pitching_stats[reliever_id], not pitching_team.pitching_stats[starter_id]. This test belongs in tests/test_game_engine.py (engine-level concern) and avoids spinning up Textual.
    - Test 2 (TestPitcherChangeEffectsSim::test_state_pitcher_id_resolves_per_inning_half): With state.half=InningHalf.TOP, home_pitcher_id is the one used (home team fielding). With state.half=InningHalf.BOTTOM, away_pitcher_id is used. Assert by direct property access (state.current_pitcher_id returns the correct id).
  </behavior>
  <action>
    In src/tui/screens/game_screen.py:advance_game, replace the line `pitcher_id = pitching_team.lineup.starting_pitcher_id` with a lookup against GameState. Specifically: when state.half == InningHalf.TOP use state.home_pitcher_id; when BOTTOM use state.away_pitcher_id (mirrors the if/else already present at lines 389-394). Equivalent: pitcher_id = state.current_pitcher_id. Then pitcher_stats = pitching_team.pitching_stats[pitcher_id] as before. If state.current_pitcher_id is None (e.g. game not initialized), fall back to lineup.starting_pitcher_id and log a warning to the play log — this only happens if _finalize_game_setup hasn't run. Do NOT change the rest of advance_game. Add the two TestPitcherChangeEffectsSim tests to tests/test_game_engine.py using direct GameState construction (no Textual import). Do not touch the inner _log_play pitcher lookup at lines 446-453 in this task — it already reads state.home_pitcher_id / state.away_pitcher_id correctly.
  </action>
  <verify>
    <automated>pytest tests/test_game_engine.py::TestPitcherChangeEffectsSim -x -v</automated>
    <automated>grep -n "starting_pitcher_id" src/tui/screens/game_screen.py | grep -v "build_lineup\|GameState(" | grep -v "# fallback"</automated>
    <automated>pytest tests/ -x</automated>
  </verify>
  <acceptance_criteria>
    - src/tui/screens/game_screen.py:advance_game no longer reads pitching_team.lineup.starting_pitcher_id for the runtime pitcher (grep above must not show advance_game's pitcher_id assignment using that path)
    - src/tui/screens/game_screen.py contains the token "state.home_pitcher_id" or "state.current_pitcher_id" inside advance_game
    - pytest tests/test_game_engine.py::TestPitcherChangeEffectsSim::test_advance_game_uses_state_pitcher_after_substitution exits 0
    - pytest tests/test_game_engine.py::TestPitcherChangeEffectsSim::test_state_pitcher_id_resolves_per_inning_half exits 0
    - Full pytest tests/ suite passes (no regressions in 100+ existing tests)
  </acceptance_criteria>
  <done>
    advance_game resolves pitcher from state. Both tests pass. No regressions. After this task, a pitcher swap recorded into GameState immediately affects the next at-bat's stats lookup.
  </done>
</task>

</tasks>

<threat_model>
Local single-player TUI game with no network surface, no auth, no user-supplied data ingestion at runtime. Threats N/A for local TUI logic. No external packages added in this plan (uses existing apply_fatigue_modifier and stdlib only).
</threat_model>

<verification>
1. `pytest tests/test_game_engine.py -x -v` — all tests including new TestFatigueEffectsSim and TestPitcherChangeEffectsSim pass
2. `pytest tests/ -x` — full suite green
3. `grep -n "apply_fatigue_modifier" src/game/engine.py` — shows definition AND at least one call site
4. `grep -n "pitching_team.lineup.starting_pitcher_id" src/tui/screens/game_screen.py` — only in fallback branch (if present) or absent entirely from advance_game
</verification>

<success_criteria>
- After a pitching change recorded into GameState, the very next at-bat simulation uses the new pitcher's stats
- Fatigue modifier is applied to pitching stats before each simulate_at_bat call
- All new and existing tests pass
- No public API signatures changed (backward-compatible)
</success_criteria>

<output>
Create `.planning/phases/06-substitution-wiring-fixes/06-01-SUMMARY.md` when done — describe both fixes, the test coverage added, and confirm via the verification commands above.
</output>
