---
phase: 06-substitution-wiring-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/game/substitutions.py
  - src/game/engine.py
  - tests/test_substitutions.py
  - tests/test_game_engine.py
autonomous: true
requirements:
  - SUBS-03
tags: [substitutions, dh-forfeiture, engine, mlb-rules]

must_haves:
  truths:
    - "When a substitution assigns the current DH to a fielding position, the manager flips home_dh_active or away_dh_active to False"
    - "When a pitching change places the new pitcher into a non-pitcher field position (PITCHER → e.g. FIRST_BASE), DH is forfeited for that team"
    - "GameEngine.make_substitution() detects forfeiture conditions and records dh_forfeited=True on the SubstitutionRecord before calling record_substitution"
    - "Once a team's DH is forfeited, the team's batting team flag stays inactive for the remainder of the game"
  artifacts:
    - path: "src/game/substitutions.py"
      provides: "would_forfeit_dh() extended to detect DH-takes-field case from lineup context"
      contains: "would_forfeit_dh"
    - path: "src/game/engine.py"
      provides: "make_substitution() invokes would_forfeit_dh and stamps dh_forfeited on SubstitutionRecord"
      contains: "dh_forfeited"
    - path: "tests/test_substitutions.py"
      provides: "Tests for both forfeiture trigger paths (DH-to-field, pitcher-to-field-position)"
      contains: "test_dh_to_field_position_forfeits_dh"
  key_links:
    - from: "src/game/engine.py:make_substitution"
      to: "src/game/substitutions.py:would_forfeit_dh"
      via: "pre-record forfeiture check"
      pattern: "would_forfeit_dh\\("
    - from: "SubstitutionRecord.dh_forfeited"
      to: "SubstitutionManager.away_dh_active / home_dh_active"
      via: "record_substitution flips the flag"
      pattern: "dh_forfeited"
---

<objective>
Close audit gap 6: DH forfeiture is currently dead code. The SubstitutionManager has the `would_forfeit_dh` method and the `dh_forfeited` flag on SubstitutionRecord, but neither GameEngine.make_substitution() nor the TUI ever sets dh_forfeited=True. This plan wires the detection so a substitution that (a) moves the current DH to a fielding position, or (b) sends a pitcher into a non-pitcher field slot, correctly forfeits the DH for the responsible team.

Purpose: Close audit gap 6 (SUBS-03). Plan 03 will then surface this through the TUI's substitution flow.
Output: would_forfeit_dh detects the DH-takes-field case using lineup context; make_substitution stamps dh_forfeited on the record; targeted tests prove both forfeiture paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-substitutions-advanced-mechanics/04-02-SUMMARY.md
@.planning/phases/04-substitutions-advanced-mechanics/04-03-SUMMARY.md
@src/game/substitutions.py
@src/game/engine.py
@src/game/positions.py
@src/game/team.py
@src/game/state.py
@tests/test_substitutions.py
@tests/test_game_engine.py

<interfaces>
<!-- Current contracts (from codebase) and what this plan extends -->

From src/game/positions.py:
  - Position(IntEnum) — PITCHER=1 ... RIGHT_FIELD=9; .is_infield, .is_outfield, .abbreviation
  - DesignatedHitter — sentinel CLASS (not enum member). Check via `slot.position is DesignatedHitter`.
  - .abbreviation == 'DH' for the sentinel; Position.PITCHER.abbreviation == 'P'

From src/game/substitutions.py — currently:
  - SubstitutionRecord(... dh_forfeited: bool = False)  (field exists, never set True)
  - SubstitutionManager.would_forfeit_dh(is_away_team, sub_type, position_change) -> bool
      Currently returns True only for PITCHING_CHANGE + non-None position_change. Misses the DH-takes-field case. (See lines 179-214; the comment explicitly notes lineup context is needed.)
  - SubstitutionManager.record_substitution(record) — already flips away/home_dh_active when record.dh_forfeited == True (lines 132-139), based on record.half (InningHalf.TOP -> away, BOTTOM -> home). This is correct and reused as-is.

From src/game/engine.py:GameEngine.make_substitution — currently constructs SubstitutionRecord with hardcoded `dh_forfeited=False` at both branches (lines 232 and 266). Needs to invoke would_forfeit_dh and pass the result.

From src/game/team.py:Lineup.slots — list of LineupSlot(player_id, position, batting_stats). position can be Position member OR DesignatedHitter sentinel.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend would_forfeit_dh to detect DH-takes-field</name>
  <files>src/game/substitutions.py, tests/test_substitutions.py</files>
  <read_first>
    - src/game/substitutions.py (would_forfeit_dh at lines 179-214; SubstitutionManager state at lines 80-95; record_substitution at lines 122-139 — already handles the flag flip)
    - src/game/positions.py (DesignatedHitter sentinel pattern — class identity check, NOT instance)
    - tests/test_substitutions.py (existing test_dh_forfeiture_for_away_team / test_dh_forfeiture_for_home_team / test_would_forfeit_dh_for_pitcher_entering_lineup for the canonical patterns)
  </read_first>
  <behavior>
    - Test 1 (test_would_forfeit_dh_for_dh_taking_field_position): Call would_forfeit_dh(is_away_team=True, sub_type=SubstitutionType.DEFENSIVE_REPLACEMENT, position_change=Position.LEFT_FIELD, old_position=DesignatedHitter). Returns True (DH is being moved into the field).
    - Test 2 (test_would_forfeit_dh_pitcher_to_pitcher_does_not_forfeit): Call would_forfeit_dh(is_away_team=True, sub_type=SubstitutionType.PITCHING_CHANGE, position_change=Position.PITCHER, old_position=Position.PITCHER). Returns False (a plain pitching change with no position swap does NOT forfeit DH).
    - Test 3 (test_would_forfeit_dh_dh_to_dh_does_not_forfeit): Call would_forfeit_dh with old_position=DesignatedHitter, position_change=DesignatedHitter (pinch hitter replacing the DH and staying as DH). Returns False.
    - Test 4 (test_would_forfeit_dh_pitcher_to_field_position): The existing test for pitcher taking field position still passes when called with the new signature (old_position=Position.PITCHER). Backward compatibility preserved.
    - Test 5 (test_would_forfeit_dh_returns_false_when_dh_inactive_with_new_signature): Existing test still passes after the signature change.
  </behavior>
  <action>
    Extend the signature of SubstitutionManager.would_forfeit_dh to accept an `old_position: Optional[Union[Position, type]] = None` parameter (default None for backward compat). Update the body to additionally return True when old_position is DesignatedHitter (the class itself, not an instance — check via `old_position is DesignatedHitter`) AND position_change is a Position member (i.e. the DH is taking a field slot, not just being replaced as DH). Also tighten the existing pitcher-to-field path: only forfeit when sub_type == PITCHING_CHANGE and position_change is a Position member other than Position.PITCHER (currently it forfeits for ANY non-None position_change including PITCHER-to-PITCHER which is wrong per test_dh_forfeiture_for_away_team's intent). Update the docstring to document the two trigger paths. Add the five tests above to tests/test_substitutions.py.
  </action>
  <verify>
    <automated>pytest tests/test_substitutions.py::test_would_forfeit_dh_for_dh_taking_field_position -x -v</automated>
    <automated>pytest tests/test_substitutions.py::test_would_forfeit_dh_pitcher_to_pitcher_does_not_forfeit -x -v</automated>
    <automated>pytest tests/test_substitutions.py -x</automated>
  </verify>
  <acceptance_criteria>
    - src/game/substitutions.py:would_forfeit_dh signature includes old_position parameter
    - grep -n "DesignatedHitter" src/game/substitutions.py returns at least one match (the new branch must reference the sentinel)
    - pytest tests/test_substitutions.py::test_would_forfeit_dh_for_dh_taking_field_position exits 0
    - pytest tests/test_substitutions.py::test_would_forfeit_dh_pitcher_to_pitcher_does_not_forfeit exits 0
    - pytest tests/test_substitutions.py — all 16 existing tests still pass (the existing test_would_forfeit_dh_for_pitcher_entering_lineup uses position_change=Position.FIRST_BASE which must still return True)
  </acceptance_criteria>
  <done>
    would_forfeit_dh recognizes both forfeiture paths and rejects the pitcher-stays-pitcher non-trigger. Five new tests pass; 16 existing tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: make_substitution stamps dh_forfeited on SubstitutionRecord</name>
  <files>src/game/engine.py, tests/test_game_engine.py</files>
  <read_first>
    - src/game/engine.py:GameEngine.make_substitution (lines 165-270, both branches construct SubstitutionRecord with dh_forfeited=False hardcoded)
    - src/game/substitutions.py:would_forfeit_dh (signature after Task 1 — includes old_position)
    - src/game/state.py (InningHalf.TOP -> away team, BOTTOM -> home team — used to derive is_away_team)
    - tests/test_game_engine.py (existing patterns; make_lineup() returns Lineup with one DesignatedHitter slot at index 8)
    - tests/test_substitutions.py (record_substitution behavior for dh_forfeited=True from existing tests test_dh_forfeiture_for_away_team)
  </read_first>
  <behavior>
    - Test 1 (TestMakeSubstitutionForfeitsDH::test_pitcher_to_field_position_forfeits_dh): Build a Team with lineup containing a pitcher at slot 0 (Position.PITCHER) and an active DH at slot 8. Build state with half=BOTTOM (home batting -> home team making sub). Call engine.make_substitution(state, team, is_away_team=False, player_out_id=<DH player>, player_in_id=<pitcher>, new_position=Position.FIRST_BASE, is_pitching_change=False). After the call, sub_manager.home_dh_active must be False. The recorded SubstitutionRecord (sub_manager.substitution_history[-1]) must have dh_forfeited == True.
    - Test 2 (TestMakeSubstitutionForfeitsDH::test_plain_pitching_change_does_not_forfeit): With same setup, call make_substitution(..., is_pitching_change=True, new_position=None). After the call, both dh_active flags remain True; recorded dh_forfeited == False.
    - Test 3 (TestMakeSubstitutionForfeitsDH::test_pinch_hitter_for_dh_no_forfeit): Replace the DH-slot player with another batter who stays as DH (new_position=None or DesignatedHitter). dh_active stays True; dh_forfeited == False.
    - Test 4 (TestMakeSubstitutionForfeitsDH::test_inning_half_determines_team_forfeited): With half=TOP, a forfeiting substitution flips away_dh_active (not home). With half=BOTTOM, flips home_dh_active. Use the same team object for both — the manager's record_substitution logic already keys on record.half.
  </behavior>
  <action>
    In src/game/engine.py:GameEngine.make_substitution, BEFORE constructing each SubstitutionRecord, derive is_away_team from `state.half == InningHalf.TOP` (per the manager's existing convention). Look up the outgoing player's current position from team.lineup.slots when the substitution is for a position player — find the slot where slot.player_id == player_out_id, capture slot.position as old_position. For pitching changes, old_position is Position.PITCHER. Call self.sub_manager.would_forfeit_dh(is_away_team=is_away_team, sub_type=<the SubstitutionType being recorded>, position_change=new_position, old_position=old_position) and use that boolean as the dh_forfeited argument when constructing SubstitutionRecord (in BOTH branches of make_substitution — pitching change branch around line 222 and position player branch around line 256). If sub_manager is None, skip the lookup and keep dh_forfeited=False as today. Do not change the function signature. Add TestMakeSubstitutionForfeitsDH class to tests/test_game_engine.py with the four tests above. Use a SubstitutionManager(away_uses_dh=True, home_uses_dh=True) injected into GameEngine.
  </action>
  <verify>
    <automated>pytest tests/test_game_engine.py::TestMakeSubstitutionForfeitsDH -x -v</automated>
    <automated>pytest tests/test_game_engine.py -x</automated>
    <automated>pytest tests/test_substitutions.py -x</automated>
    <automated>grep -n "would_forfeit_dh" src/game/engine.py</automated>
  </verify>
  <acceptance_criteria>
    - src/game/engine.py contains at least two call sites for sub_manager.would_forfeit_dh (one per branch in make_substitution) — grep above must return non-empty
    - pytest tests/test_game_engine.py::TestMakeSubstitutionForfeitsDH::test_pitcher_to_field_position_forfeits_dh exits 0
    - pytest tests/test_game_engine.py::TestMakeSubstitutionForfeitsDH::test_plain_pitching_change_does_not_forfeit exits 0
    - pytest tests/test_game_engine.py::TestMakeSubstitutionForfeitsDH::test_pinch_hitter_for_dh_no_forfeit exits 0
    - pytest tests/test_game_engine.py::TestMakeSubstitutionForfeitsDH::test_inning_half_determines_team_forfeited exits 0
    - Full pytest tests/test_game_engine.py and pytest tests/test_substitutions.py suites pass (no regressions)
  </acceptance_criteria>
  <done>
    Both branches of make_substitution invoke would_forfeit_dh and stamp the boolean on the record. Manager's dh_active flag flips correctly for forfeiting subs and stays True for benign ones. Four new tests pass.
  </done>
</task>

</tasks>

<threat_model>
Local single-player TUI game with no network surface, no auth, no user-supplied data ingestion at runtime. Threats N/A for local TUI logic. No new packages.
</threat_model>

<verification>
1. `pytest tests/test_substitutions.py -x -v` — all (existing 16 + 5 new) tests pass
2. `pytest tests/test_game_engine.py::TestMakeSubstitutionForfeitsDH -x -v` — 4 new tests pass
3. `pytest tests/ -x` — full suite green (no cross-test regressions)
4. `grep -n "would_forfeit_dh" src/game/engine.py` — at least two call sites inside make_substitution
</verification>

<success_criteria>
- DH-takes-field substitutions correctly forfeit the DH for the substituting team
- Pitcher-to-field-position substitutions correctly forfeit the DH
- Plain pitching changes (PITCHER→PITCHER) and pinch hitters for DH (DH→DH) do NOT forfeit
- All existing tests still pass; no public signatures broken
</success_criteria>

<output>
Create `.planning/phases/06-substitution-wiring-fixes/06-02-SUMMARY.md` when done — describe the two-path detection, the test matrix, and confirm verification.
</output>
